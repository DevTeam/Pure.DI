<Project>
    <PropertyGroup>
        <Configuration Condition="'$(Configuration)'==''">Debug</Configuration>
        <Version Condition="'$(Version)'==''">1.0.0</Version>
    </PropertyGroup>

    <UsingTask TaskName="Pure.DI.MSBuild.Tasks.Merge" AssemblyFile="Pure.DI.MSBuild.Tasks/bin/netstandard2.0/Pure.DI.MSBuild.Tasks.dll"/>
    <Target Name="Build">
        <Merge Path="../Pure.DI/bin/$(Configuration)" Version="$(Version)"/>
    </Target>
</Project>